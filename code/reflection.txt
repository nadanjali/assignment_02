
This module helped me undersand how specific the order and syntax of files and packages work in order to have it be truly efficient when coding. When I started exporting my functions into the __init__ file, I was getting many errors while trying to write my reports because I had not exported in the proper order. I now understand that the sequence of the code must match up with the order you export into the file, and later when you load those functions from the package. 

Through the repeated practice, I was also able to fix mistakes that the chat helper had recommended since I had not orignially loaded in my packages correctly. In my marketing report, for example, I was able to get away with using  print_sales_table instead of print_item_table since they used compatible variables. It wasn't until I kept running into errors during the daily report that I realized I had missed saving an entire function.  

